#include <iostream>
#include <string>
#include <cstring>
#include <fstream>
#include <string.h>
#include <ctime>
#include <thread>
#include <chrono>
#include <mutex>

using namespace std::chrono_literals;

int main() {
    setlocale(LC_ALL, "rus");
    // std::thread t1(rec, 5);
    // std::thread t2(rec, 4);

    // t1.join();
    // t2.join();

    // std::cout << num << std::endl;

    return 0;
}